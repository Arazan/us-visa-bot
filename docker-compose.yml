services:
  bot:
    build: .
    image: us-visa-bot:latest
    container_name: us-visa-bot
    restart: unless-stopped
    env_file:
      - .env
    # All run parameters (CURRENT_DATE, TARGET_DATE, MIN_DATE, DRY_RUN,
    # MAX_LOOPS, MAX_BOOKINGS, ...) are now configured via .env.

